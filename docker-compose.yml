version: '3.8'

services:
  app:
    build: .
    ports:
      - "5005:5005"
    volumes:
      - ./server/uploads:/app/uploads
    environment:
      - NODE_ENV=production
    restart: unless-stopped